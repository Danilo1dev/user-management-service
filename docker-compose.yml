services:
  user-management-service:
    build: ./
    container_name: user-management-service
    restart: always
    ports:
      - "8086:8086"
    environment:
      - SERVER_PORT=8086
    networks:
      - microservices-network

networks:
  microservices-network:
    driver: bridge
